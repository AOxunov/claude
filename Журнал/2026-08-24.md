---
tags: [журнал, vue, easy-trip]
date: 2026-08-24
project: easy-trip
---

# Сессия 2026-08-24

## Что разобрали

- Ответ `GET /endpoint/api/v1/moderation/places` содержит URL главной фотографии объекта в поле `main_img`.
- Для списка модерации нужно отрисовывать это поле рядом с названием объекта и корректно обрабатывать `null`.
- Токен авторизации не следует сохранять в клиентском Vue-коде или в git.

## Что понял

- API-контракт использует `snake_case`, поэтому его поля нужно явно отобразить в модель интерфейса или использовать как есть.

## Что осталось непонятным

- В vault нет исходников страницы Easy Trip, поэтому правка подготовлена как точечные фрагменты для вставки в конструктор.

## Что делать в следующий раз

- Вставить привязку `main_img` в шаблон списка и проверить объекты с фотографией и без неё.
